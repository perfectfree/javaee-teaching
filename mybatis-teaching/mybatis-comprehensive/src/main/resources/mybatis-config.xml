<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/test?characterEncoding=utf-8&amp;useSSL=FALSE&amp;allowPublicKeyRetrieval=true"/>
                <property name="username" value="test"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <!-- 注册注解方式的Mapper接口 -->
        <mapper class="edu.jiangbaiyu.demo.mybatis.comprehensive.mapper.UserMapper"/>
        <mapper class="edu.jiangbaiyu.demo.mybatis.comprehensive.mapper.DeptMapper"/>

        <!-- 注册XML映射文件（如果有复杂SQL） -->
        <mapper resource="mapper/UserMapper.xml"/>
    </mappers>
</configuration>